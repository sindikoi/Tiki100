import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor:'rgba(66, 158, 233, 0.54)',
        alignItems: 'center',
        justifyContent: 'flex-start',
        paddingTop: 50,
      },

});
